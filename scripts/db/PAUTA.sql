CREATE TABLE PAUTA (
    ID                       VARCHAR NOT NULL,
    ASSUNTO                  VARCHAR NOT NULL,
    DATA_CRIACAO             TIMESTAMP     NOT NULL,

CONSTRAINT PK_PAUTA          PRIMARY KEY (ID)
);
